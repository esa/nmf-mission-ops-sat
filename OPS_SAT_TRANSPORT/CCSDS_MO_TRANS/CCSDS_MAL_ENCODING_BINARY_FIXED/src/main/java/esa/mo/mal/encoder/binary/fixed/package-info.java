/**
An implementation of the encoding API for a binary encoding. This extends the base binary encoder to use fixed length encodings rather than the varints of the base.
It produces fixed size encoded messages.
 */
package esa.mo.mal.encoder.binary.fixed;
