/**
An implementation of the encoding API for the fixed length SPP binary encoding. This extends the base fixed length
binary encoder to use the correct fixed length encodings SPP rather than the ones of the base. It produces fixed size
encoded messages.
 */
package esa.mo.mal.encoder.spp;
